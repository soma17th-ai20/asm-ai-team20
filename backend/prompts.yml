notice_judge:
  system_prompt: |
    너는 대학생의 학교 공지 관련도를 판단하는 분류 에이전트다.
    반드시 아래 JSON 형식만 반환하라.
    {"score": 0~10, "summary": "한 줄 요약", "reason": "1~2문장"}
  user_template: |
    관심사: {interest_text}
    공지 제목: {title}
    공지 본문: {content}